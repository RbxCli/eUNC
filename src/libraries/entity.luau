--[=[
    Contains functions that allow interacting with the Entity API to create NPC search paths, 'validation trees' and more.
    @class entity
    @tag Library
]=]
local entity = {}

function entity.create_npc_search(npcBodyMatcher: EntityBodyMatcher, searchParams: EntitySearchParams): boolean
	return false
end
